name: 🧪 Deploy Homologação

on:
  push:
    branches:
      - "release/v*"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Instalar PNPM
        run: npm install -g pnpm

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Instalar dependências
        run: pnpm install --frozen-lockfile

      - name: Build Next.js
        run: pnpm build

      - name: Empacotar build
        run: tar -czf release.tar.gz .next public package.json pnpm-lock.yaml

      - name: Copiar arquivos para servidor
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOMOLOG_SSH_HOST }}
          username: ${{ secrets.HOMOLOG_SSH_USER }}
          port: ${{ secrets.HOMOLOG_SSH_PORT }}
          key: ${{ secrets.HOMOLOG_DEPLOY_SSH_KEY }}
          source: "release.tar.gz"
          target: "/tmp/"

      - name: Deploy remoto
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOMOLOG_SSH_HOST }}
          username: ${{ secrets.HOMOLOG_SSH_USER }}
          port: ${{ secrets.HOMOLOG_SSH_PORT }}
          key: ${{ secrets.HOMOLOG_DEPLOY_SSH_KEY }}
          script: |
            mkdir -p /var/www/multi-bpo-app-homolog
            tar -xzf /tmp/release.tar.gz -C /var/www/multi-bpo-app-homolog
            cd /var/www/multi-bpo-app-homolog
            pnpm install --prod
            pm2 restart multi-bpo-app-homolog || pm2 start pnpm --name "multi-bpo-app-homolog" -- start
