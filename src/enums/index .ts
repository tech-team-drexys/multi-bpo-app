export enum DriveItemType {
    MONTH = 'months',
    CLIENT = 'clients',
    SERVICE = 'services',
    FILE = 'files',
}

export enum TaskStatus {
    OVERDUE = 'overdue',
    ON_TRACK = 'on_track',
    DUE_TODAY = 'due_today',
    COMPLETED = 'completed',
}